import { render, screen } from "@testing-library/react";
import UserList from "../components/UserList/UserList";

test("to verify users list", async () => {
  render(<UserList />);
  // expect(screen.get)
});
